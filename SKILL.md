---
name: ob-swift-figma
description: Implement Figma screens in the existing GeneralOB Swift iOS project with high visual fidelity, first verifying the project contains a complete GeneralOB folder with required base files and DataModel/Pages folders and copying the skill-bundled assets/GeneralOB scaffold into the project before continuing if it is missing or incomplete, automatically reading GeneralOB/OBFigma.md for queued pages, processing one page block at a time where multiple Figma links inside the same block are UI states of the same page, honoring any page-block announcement as hard implementation constraints, using retryable page status markers and a git commit after each completed page block, placing every new page under GeneralOB/Pages, naming new page files and classes as OB<Page>VC such as OBStartVC, localizing displayed copy with #Localized("") and import OOGMacroKits, making ordinary pages inherit from GeneralOBVC and option-style pages inherit from GeneralOBCollectionVC, mapping each page one-to-one with GeneralOBPage, putting option-page data in GeneralOBPage.pageData with GeneralOBPageItem(title: "abc", localizedTitle: #Localized("abc")), persisting list and non-list click-select page selection state in GeneralOBData, wiring makeSelectIndexes only for list pages in the external page.vc creation path, implementing restore/toggle/save code inside non-list click-select pages, setting GeneralOBPage multipleSelected and cellHeightIsConsistent metadata from the Figma option list behavior, overriding cell isSelected didSet in the cell subclass when selected UI differs from GeneralOBBaseCell defaults, making page content scrollable when the Figma page height is greater than 844 while keeping the bottom button fixed and floating above the scrolling content, implementing bottom continue button states only by overriding GeneralOBVC.nextBtnEnable didSet, using GeneralOBBaseCell.selectedBaseView for non-solid selected cell background styles instead of adding custom update methods, forbidding makeSelect calls inside page implementations because they belong to external VC initialization, avoiding UIImageView contentMode assignments in page code, mapping missing Figma navigation chrome to GeneralOBPage flags, customizing GeneralOBBaseCell through baseView/titleLab/icon/checkIcon and snp.remakeConstraints, using sliced image assets for GeneralOBBaseCell checkIcon selected and unselected states, never calling UICollectionView reload methods, and exporting page images as PNG 2x/3x assets into a namespaced Assets.xcassets folder for the matching GeneralOBPage while reusing current components, models, utilities, assets, pods, and coding conventions. Use when asked to turn a Figma page, node, screenshot, OBFigma.md queue, or design spec into UIKit code inside this onboarding codebase; do not use for generic iOS tutorials, brand-new apps without an existing codebase, SwiftUI-only work, or Figma-to-web work.
metadata:
  author: Yao Team
---

# OB Swift Figma

Build GeneralOB onboarding pages from Figma by first confirming the current project contains the required `GeneralOB` architecture, then learning the project and implementing inside that architecture. New Figma pages must live in `GeneralOB/Pages`; ordinary pages inherit from `GeneralOBVC`, option-style pages inherit from `GeneralOBCollectionVC`, and every page maps one-to-one with `GeneralOBPage`. Fidelity matters, but local fit wins over isolated generated code.

## Workflow

1. Before reading Figma or starting any page task, locate the engineering project root and verify it contains a complete `GeneralOB` folder with `GeneralOBCollectionVC.swift`, `GeneralOBPage.swift`, `GeneralOBPage+Data.swift`, `GeneralOBPage+VC.swift`, `GeneralOBVC.swift`, `DataModel/`, and `Pages/`. If the folder is missing or incomplete, copy the skill-bundled `assets/GeneralOB` folder into the project, re-run the same verification, and do not continue by creating stub replacement files.
2. At task start, automatically read `GeneralOB/OBFigma.md` when it exists; parse page blocks such as `## 001`, their names, all Figma URLs inside the block, any `### announcement` content, and any existing `ob-status` marker.
3. Pick the first page that is not marked `done`; if a page is marked `failed` or `in_progress`, retry that same page before moving to later pages.
4. Mark the selected page as `in_progress` in `OBFigma.md` before implementation with a lightweight status marker such as `<!-- ob-status: in_progress; attempts: 1 -->`.
5. Read every Figma URL in the selected page block with the available Figma tools. Treat multiple Figma links in one block as multiple UI states of the same page, not separate page tasks.
6. If the selected block has `### announcement`, treat every statement under it as a hard page-level implementation condition, satisfy it before marking the page `done`, and let it override generic page-shape heuristics when they conflict.
7. Inspect the iOS project before editing: `GeneralOB/Pages`, `GeneralOBPage`, `GeneralOBPage+Data.swift`, `GeneralOBPageData`, base classes, page routing, existing components, design tokens, assets, pods, and nearby screens.
8. Plan the mapping from Figma layers to current project primitives, naming every reused component, base class, helper, asset, and library, plus how each announcement condition will be satisfied.
9. Add or update exactly one `GeneralOBPage` case for each new Figma page, then map that case to exactly one page implementation.
10. Name each new page file and class as `OB<Page>VC`, where `<Page>` is the PascalCase form of the `GeneralOBPage` case, such as `OBStartVC.swift` / `OBStartVC`.
11. Implement each ordinary Figma page in `GeneralOB/Pages` as a `GeneralOBVC` subclass or by extending the existing `GeneralOBVC` page flow, using the project's existing UI style, layout system, scaling helpers, font APIs, colors, lifecycle, and state patterns.
12. For option-style pages, subclass `GeneralOBCollectionVC`, source the option model from `GeneralOBPage.pageData.items`, and override only the necessary hooks such as `registerCell` and `cell(_ c: UICollectionView, path: IndexPath)` instead of rebuilding collection view plumbing from scratch, unless the page announcement explicitly forbids `UICollectionView`.
13. For option-list pages, set `page.multipleSelected = true` when the Figma interaction supports multiple selections; otherwise leave or set it to `false`.
14. For option-list pages, set `page.cellHeightIsConsistent = true` when every list cell has the same height in the Figma design; set `page.cellHeightIsConsistent = false` when cell heights can differ because of layout, expanded state, multiline content, or per-option design differences.
15. For list pages or any non-list page with tap-to-select behavior, add a matching property to `GeneralOBData` for persisted selection state: single-select index state uses `var page: Int?`, and multi-select index state uses `var page: [Int]?`, where `page` is the project-consistent property name for that `GeneralOBPage`.
16. For list pages, restore and save selection with `vc.makeSelectIndexes(...)` only in the external `GeneralOBPage` `page.vc` creation path; keep `makeSelectIndexes` out of `OB<Page>VC`, cells, and page-local helpers.
17. For non-list pages with tap-to-select behavior, restore selection from `GeneralOBData` during page setup, update the selected UI in the page's own state-rendering code, and save changes back to `GeneralOBData` in the tap handler; do not use `makeSelectIndexes` for non-list selection.
18. Initialize option items with both raw and localized titles, for example `GeneralOBPageItem(title: "abc", localizedTitle: #Localized("abc"), icon: "...")`.
19. Never call `UICollectionView` reload methods such as `reloadData()`, `reloadItems(at:)`, or `reloadSections(_:)`; rely on `GeneralOBCollectionVC` data flow, diffable snapshots, selection APIs, or targeted state updates instead.
20. Do not call `makeSelectItems`, `makeSelectIndexes`, or any `makeSelect*` method inside the page implementation; those methods are reserved for the external code path that creates and initializes list page VCs.
21. For bottom continue button enabled, disabled, color, title color, or other state differences, override the inherited `nextBtnEnable` property and implement the state changes in its `didSet`; do not create a new helper method such as `updateNextButtonState`.
22. When using `UIImageView` in page code, create it without assigning `contentMode`; do not set `.scaleAspectFit`, `.scaleAspectFill`, `.center`, or any other `contentMode` in the page implementation.
23. When subclassing `GeneralOBBaseCell`, add new controls to `baseView`, prefer the inherited `titleLab`, `icon`, `checkIcon`, and `selectedBaseView`, implement `checkIcon` selected and unselected states with image assets only, call `super.setupUI()` in overridden `setupUI`, and use SnapKit `snp.remakeConstraints` when adjusting inherited constraints.
24. If a list cell's selected UI differs from the base class behavior, override `isSelected` in the cell subclass and apply the correction in `didSet`, using inherited controls such as `selectedBaseView`, `checkIcon`, `titleLab`, and `icon` where possible.
25. Localize every user-visible string with `#Localized("...")` and add `import OOGMacroKits` at the top of Swift files that use the macro.
26. Add or update assets only when they are present in Figma or explicitly required; export Figma images as PNG resources with `2x` and `3x` variants, not SVG; put each page's exported image assets inside the matching `GeneralOBPage` folder under the `GeneralOB` asset namespace.
27. Keep title and subtitle layers above all other page content, including images, collection views, cards, decorations, gradients, and background art.
28. When the Figma page frame height is greater than `844`, put page content into a scrollable area while keeping the bottom continue button outside that scroll view so it stays fixed and visually floats above scrolling content.
29. Map Figma chrome visibility to `GeneralOBPage` metadata: no top-left back button means `page.canBack = false`, no top progress bar means `page.isHideProgress = true`, and no bottom continue button means `page.isHideContinueBtn = true`.
30. Verify compile-time integration, layout responsiveness, safe areas, iPad or small-device variants, visual fidelity against the Figma screenshot, scroll behavior for frames taller than `844`, persisted selection restore/save behavior for both list and non-list selectable pages, cell subclass `isSelected.didSet` corrections when required, and all announcement conditions.
31. On success, mark the page `done` in `OBFigma.md`, stage only the current page's implementation, asset, project, and status-marker changes, create one git commit for that page, then continue to the next not-done page.
32. On failure, mark the page `failed` in `OBFigma.md` with the attempt count and short reason, do not continue to later pages, and retry the current page from its status marker on the next run.
33. Summarize changed files, reused project primitives, page status updates, commit hashes, announcement conditions and how they were satisfied, any fidelity compromises, and verification results.

## Project Fit Rules

- When `GeneralOB/OBFigma.md` exists, treat it as the page queue for this skill. Parse page blocks in document order, preserve user-authored text and Figma links, and edit only the status marker lines needed for execution state.
- Before queue parsing, Figma reading, or Swift editing, confirm the project has a complete `GeneralOB` folder with the required base files and folders. If not, copy the skill-bundled `assets/GeneralOB` folder into the engineering project and verify the required structure again before continuing.
- A single `OBFigma.md` page block may contain multiple Figma links. All links in the same block belong to the same `GeneralOBPage` case and `OB<Page>VC` implementation, and represent different UI display states such as selected, disabled, expanded, empty, error, or alternate content states. Do not split them into separate pages, enum cases, files, or commits.
- A page block may contain a `### announcement` section. Treat all text under that heading until the next heading of the same or higher level as hard page-level implementation constraints for that page. Satisfy every announcement condition before marking the page `done`, and report how each condition was met.
- If a page announcement conflicts with generic inference from Figma or with a reusable default in this skill, the announcement wins for that page unless it violates non-negotiable project boundaries such as UIKit GeneralOB integration, localization, or asset safety. Call out any such conflict instead of silently choosing the generic rule.
- For example, an announcement of `不使用UICollectionView` means the page must not use `UICollectionView`, `GeneralOBCollectionVC`, collection cells, or collection reload/update APIs, even if the Figma layout looks like a selectable option list. Implement it as an ordinary `GeneralOBVC` page or another existing non-collection project pattern while still preserving `GeneralOBPage` mapping and page data rules that remain applicable.
- Use a lightweight page status marker inside each `OBFigma.md` page block, for example `<!-- ob-status: todo -->`, `<!-- ob-status: in_progress; attempts: 1 -->`, `<!-- ob-status: failed; attempts: 1; reason: build failed -->`, or `<!-- ob-status: done; commit: <hash> -->`. Missing status means `todo`.
- Complete and commit one `OBFigma.md` page block at a time. After a page passes verification, update its status to `done`, run `git status` and `git diff`, stage only files belonging to that page plus the `OBFigma.md` marker, commit with a page-specific message such as `Implement OB 001 body reading begins`, then proceed to the next page.
- If the current page fails, update only that page's marker to `failed` with an attempt count and short reason, keep enough context for automatic retry, and retry that same page before processing any later page. Do not mark later pages or skip ahead while a failed page remains.
- Prefer existing local components, `GeneralOBVC`, `GeneralOBCollectionVC`, extensions, and pods over new abstractions.
- Do not implement these Figma pages as standalone `UIViewController`, direct `OBBaseViewController` subclasses, or SwiftUI screens unless the user explicitly changes this skill's boundary.
- Place all new Figma page implementation files under `GeneralOB/Pages`; place reusable page data models under `GeneralOB/Pages/Model` only when the existing `GeneralOBPageData` shape is insufficient.
- New page file names and class names must use `OB<Page>VC`, where `<Page>` follows the `GeneralOBPage` case in PascalCase; for example `.start` maps to `OBStartVC.swift` and `OBStartVC`.
- Every user-visible string must be wrapped with `#Localized("...")`, including labels, button titles, option titles, tag titles, cell text, subtitles, descriptions, and `GeneralOBPage.pageData` text; any Swift file using that macro must include `import OOGMacroKits` near the top.
- For this project family, every new Figma onboarding page should inherit from `GeneralOBVC` and continue to use `GeneralOBPage`, `GeneralOBPage.pageData`, `OBPage`, `OnboardingPagesDataSource`, `Components`, `SnapKit`, and `OOGFontKit` patterns.
- Keep `GeneralOBPage` and page implementations one-to-one: one enum case owns one page class or one clearly isolated `GeneralOBVC` rendering branch.
- Match Figma navigation chrome with `GeneralOBPage` flags: if the design has no top-left back button, set that page's `canBack` to `false`; if it has no top progress bar, set `isHideProgress` to `true`; if it has no bottom continue button, set `isHideContinueBtn` to `true`.
- For option-style Figma pages, inherit from `GeneralOBCollectionVC`; define option data in `GeneralOBPage.pageData` with `GeneralOBPageData` and `GeneralOBPageItem`; override `registerCell` and `cell(_ c: UICollectionView, path: IndexPath)` for the page-specific cell; do not create a fresh collection view data source, delegate stack, or selection pipeline.
- If the current page announcement forbids `UICollectionView`, do not apply the option-style `GeneralOBCollectionVC` rule for that page. Use a non-collection implementation and explicitly verify that no `UICollectionView` symbols were introduced in the page-specific files.
- For option-list Figma pages, set `page.multipleSelected = true` when the page supports multi-select; otherwise ensure it remains `false`.
- For option-list Figma pages, set `page.cellHeightIsConsistent = true` when all collection cells have the same rendered height; set `page.cellHeightIsConsistent = false` when Figma shows variable heights or any selection/content state can change cell height.
- For list pages or any non-list page with tap-to-select behavior, add a matching persisted selection property to `GeneralOBData`. Use `var page: Int?` for single-select index state and `var page: [Int]?` for multi-select index state, where `page` is replaced with the project-approved property name for the `GeneralOBPage` case.
- For list pages, in the external `page.vc` creation path for the matching `GeneralOBPage`, restore and save selected index paths with `vc.makeSelectIndexes`. Single-select restores `[IndexPath(item: data, section: 0)]` from `GeneralOBData.shared.page` and saves `arr.first?.item`; multi-select restores `data.map { IndexPath(item: $0, section: 0) }` and saves the selected indexes array.
- List-page selection restore/save belongs in the external `GeneralOBPage` `page.vc` or equivalent VC construction location. Do not put `makeSelectIndexes`, `makeSelectItems`, or any `makeSelect*` call inside `OB<Page>VC`, custom cells, or page-local selection/layout code.
- For non-list pages with tap-to-select behavior, do not use `makeSelectIndexes`. Restore the current value from `GeneralOBData` when building the page UI, render the selected/unselected state through a page-local state method or property, and update `GeneralOBData` whenever the user taps to change selection.
- Do not call `UICollectionView` reload APIs anywhere in Figma page implementations, including `reloadData()`, `reloadItems(at:)`, `reloadSections(_:)`, or wrapper helpers that call them. Use the existing `GeneralOBCollectionVC` diffable data source, `mainPage.pageData.items`, selection methods, and direct cell state updates instead.
- Do not call `makeSelectItems`, `makeSelectIndexes`, or any `makeSelect*` method from inside `OB<Page>VC`, cells, or page-local layout/selection code. For list pages, initial selection setup through `makeSelect*` belongs only to the external page creation or VC initialization location; for non-list tap-to-select pages, restore and save selection directly through `GeneralOBData`.
- Customize bottom continue button states by overriding `nextBtnEnable` and putting all enabled/disabled UI changes in its `didSet`, following `GeneralOBVC` and nearby pages such as `OBSexVC`. Do not add a new method or helper solely to refresh bottom button state.
- Do not assign `contentMode` on `UIImageView` instances in page implementations, page-local custom views, or cells; create image views and set their image/constraints without `.contentMode = ...`.
- Initialize every option `GeneralOBPageItem` with a raw `title` and localized `localizedTitle`, using the form `GeneralOBPageItem(title: "abc", localizedTitle: #Localized("abc"), icon: "...")`; do not set only `localizedTitle`, and do not place option display strings directly in cells.
- When creating a custom cell by subclassing `GeneralOBBaseCell`, add any new labels, image views, buttons, or containers to `baseView`; prefer reusing inherited `titleLab`, `icon`, `checkIcon`, and `selectedBaseView` before adding replacements; implement `checkIcon` selected and unselected states with cut image resources such as `image` and `highlightedImage`, never with pure colors, borders, drawn shapes, SF Symbols, or runtime vector drawing; if the inherited layout does not match Figma, update those controls with `snp.remakeConstraints`; any overridden `setupUI` must call `super.setupUI()`.
- If the current list page's selected cell UI does not match the base class selected-state behavior, override `isSelected` in the custom cell subclass and apply the UI correction in `didSet`. Keep the correction cell-local and use inherited controls such as `selectedBaseView`, `checkIcon`, `titleLab`, and `icon`; do not move selected UI correction into the page VC or external selection persistence code.
- If an option-list cell's selected background style is not a pure color, such as a gradient, image, textured fill, multi-layer shape, or other complex Figma background, implement that selected background through the inherited `GeneralOBBaseCell.selectedBaseView`. Do not add a separate method such as `updateSelectedBackground`, `refreshSelectedStyle`, or a page-specific selected-background toggle method just to handle that state.
- When Figma reports a component corner radius of `99` or `999`, treat it as a capsule shape and set the Swift corner radius to half of the component height, such as `height / 2` or `cx390(height) / 2`, instead of copying `99` or `999`.
- Exported Figma images for a page must use PNG format with `2x` and `3x` variants; do not export or commit SVG for these page assets. Store them with the `GeneralOB/<page>/...` asset namespace pattern under `GeneralOB/Assets.xcassets/GeneralOB/<page>/`, where `<page>` matches the `GeneralOBPage` case or project-approved page asset name; enable `provides-namespace` for both `GeneralOB` and `<page>` folders; reference images with namespaced asset names such as `UIImage(named: "GeneralOB/<page>/<assetName>")`.
- Title and subtitle views must have a higher visual layer than all other content. Add them after background/content views or call `view.bringSubviewToFront(...)` / set an appropriate `layer.zPosition` so they cannot be covered by images, collection cells, decorative overlays, or gradients.
- If the Figma page frame height is greater than `844`, the page's main content must scroll. Use the project's existing scroll pattern, such as `UIScrollView` plus a content view or the inherited collection view when allowed, and constrain the scrollable content below the top chrome/title area and above the bottom button area.
- For pages taller than `844`, keep the bottom continue button outside the scrollable content and attached to the existing `GeneralOBVC` bottom button layer or a fixed bottom overlay. The button must not move with content scrolling, and it should visually float above the scroll view with enough bottom inset/content inset so the last content is not hidden under the button.
- Use `rg` to locate helpers such as `cx390`, `UIColor("#...")`, `cornerRadius`, typography extensions, asset names, routers, and existing reusable controls before writing substitutes.
- Do not overwrite user code, remove unrelated files, or change project architecture just to match a Figma frame.

## Output Contract

- Produce working Swift code integrated into the current Xcode project.
- Confirm the preflight found or restored a complete `GeneralOB` folder containing `GeneralOBCollectionVC.swift`, `GeneralOBPage.swift`, `GeneralOBPage+Data.swift`, `GeneralOBPage+VC.swift`, `GeneralOBVC.swift`, `DataModel/`, and `Pages/` before any Figma implementation began.
- If the target project was missing this structure, confirm it was restored from the skill-bundled `assets/GeneralOB` scaffold.
- Include exact file references for implementation and assets.
- For `OBFigma.md` batch execution, report the page id, page title, previous status, final status, attempt count, and commit hash for each completed page.
- If the completed `OBFigma.md` block contains multiple Figma links, report them as UI states of the same implemented page and summarize how each state was handled.
- If the completed `OBFigma.md` block contains `### announcement`, list every announcement condition and confirm the implementation satisfies it before claiming `done`.
- Confirm new page files are under `GeneralOB/Pages` and that `GeneralOBPage` maps one-to-one to the implemented page.
- Confirm each new page file and class follows the `OB<Page>VC` naming rule.
- Confirm displayed strings use `#Localized("...")` and each file using it imports `OOGMacroKits`.
- Confirm `GeneralOBPage` flags match Figma chrome visibility for back button, progress bar, and bottom continue button.
- For option-style pages, confirm the page inherits from `GeneralOBCollectionVC`, overrides `registerCell` and `cell(_ c: UICollectionView, path: IndexPath)`, and reads options from `mainPage.pageData.items`.
- For list pages or non-list pages with tap-to-select behavior, confirm `GeneralOBData` has the matching persisted selection property.
- For list pages, confirm `page.vc` restores and saves selection through `vc.makeSelectIndexes`.
- For non-list tap-to-select pages, confirm the page restores selection from `GeneralOBData`, updates selected UI through page-local state, saves changes back to `GeneralOBData` in tap handlers, and does not call `makeSelectIndexes`.
- Confirm option-list page metadata matches Figma behavior: multi-select means `page.multipleSelected = true`; same-height cells mean `page.cellHeightIsConsistent = true`; variable-height cells mean `page.cellHeightIsConsistent = false`.
- Confirm no `UICollectionView` reload methods are called, including `reloadData()`, `reloadItems(at:)`, and `reloadSections(_:)`.
- Confirm the page implementation does not call `makeSelectItems`, `makeSelectIndexes`, or any `makeSelect*` method; for list pages, confirm initial selection is wired by the external `page.vc` initialization code; for non-list click-select pages, confirm restoration and saving happen through `GeneralOBData` in the page's own state/tap logic without `makeSelect*`.
- Confirm bottom continue button state differences are implemented through `override var nextBtnEnable` with `didSet`, not a newly created refresh/update method.
- Confirm no page implementation, page-local custom view, or cell assigns `UIImageView.contentMode`.
- Confirm option data initializes `GeneralOBPageItem` with both `title: "abc"` and `localizedTitle: #Localized("abc")`.
- If a custom option cell subclasses `GeneralOBBaseCell`, confirm new controls were added to `baseView`, inherited `titleLab`, `icon`, `checkIcon`, and `selectedBaseView` were reused where possible, `checkIcon` selected and unselected states use cut image assets, `setupUI` calls `super.setupUI()`, and inherited constraints were adjusted with `snp.remakeConstraints` when needed.
- If the list cell selected UI differs from the base class behavior, confirm the custom cell subclass overrides `isSelected` and applies the UI correction in `didSet`.
- If a selected cell background is not a pure color, confirm it is implemented with `selectedBaseView` and not with a newly added selected-background update method.
- Confirm Figma corner radius values of `99` or `999` were implemented as half-height capsule radii.
- Confirm exported image assets are PNG `2x` and `3x` resources, not SVG, stored in the matching `GeneralOB/<page>/...` namespaced `Assets.xcassets` page folder and referenced through that namespace.
- Confirm title and subtitle layers are above images, collection views, cards, decoration, and background art.
- If the Figma page height is greater than `844`, confirm the main content scrolls and the bottom continue button stays fixed/floating above the scrollable content instead of being inside the scroll view.
- Report the Figma-to-code mapping: layers, components, fonts, colors, spacing, images, interactions, and states.
- State which local components, base classes, pods, and helpers were reused.
- Include verification commands or Xcode build results; if a build cannot run, say why and list manual checks performed.

## Reference Map

- Read `references/implementation-workflow.md` for the execution checklist.
- Read `reports/output-risk-profile.md` before final delivery.
